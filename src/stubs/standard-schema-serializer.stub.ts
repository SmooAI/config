/* eslint-disable @typescript-eslint/no-explicit-any -- stub */
// Stub for browser builds — replaces schema serialization libraries that are Node.js-only

// @valibot/to-json-schema
export const toJsonSchema = (..._args: any[]) => ({});
export const valibotToJsonSchema = (..._args: any[]) => ({});

// effect
export const Schema = { isSchema: () => false };

// effect/JSONSchema
export const make = (..._args: any[]) => ({});

// json-schema-to-zod
export const jsonSchemaToZod = (..._args: any[]) => '';
